import 'package:shared_preferences/shared_preferences.dart';

/// WORK IN PROGRESS
class PersistentDataSource {
  // final _sharedPrefs;

  // PersistentDataSource() : _sharedPrefs = await SharedPreferences.getInstance();
}
